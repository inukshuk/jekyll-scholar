source 'https://rubygems.org'
gemspec

gem 'cucumber'
gem 'minitest'
gem 'rake'

group :coverage do
  gem 'simplecov', :require => false
end

group :debug do
  gem 'debug', '>= 1.0.0', :require => false
end
